#include <stdio.h>

int main()
{
    printf("Chapter 5 : Functions & Recursion");

    // Function is a block of code that performs a purticular task. It can be used multiple times and increase code reusability

    // Recursion is a programming technique where a function calls itself to solve a problem by breaking it down into smaller, similar sub-problems.

    return 0;
}
