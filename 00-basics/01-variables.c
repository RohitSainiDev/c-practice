#include <stdio.h>

int main()
{
    // Declaring and initialising variables
    int number = 25;
    char hash = '#';
    int age = 25;
    float g = 9.8;

    // Variable naming rules

    // 1. They are case sensitive
    int var = 0101;
    int Var = 0234;

    // 2. First character is alphabet or underscore(_)
    int _age = 11;

    // 3. No comma or space allowed use underscore to assign meaningful names
    int final_price = 100;

    // 4. No special character other than underscore allowed. Use meaningful names

    return 0;
}