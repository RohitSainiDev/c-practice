#include <stdio.h>

int main()
{
    int is_sunday = 1;
    int is_snowing = 1;

    printf("Whether the day is sunday and it is snowing(1 = true, 0 = false):%d \n", is_snowing && is_sunday);

    return 0;
}